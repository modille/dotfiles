# Set the layout for the current tab
layout tall
# Set the working directory for windows in the current tab
cd ~/Dropbox/obsidian
## Create the "main" window and run an editor in it
launch fish -c nvim

# Create a new tab
new_tab
layout tall
cd ~/
